apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: ravkav-geo-dev
spec:
  replicas: 1
  selector: { matchLabels: { app: admin } }
  template:
    metadata: { labels: { app: admin } }
    spec:
      containers:
        - name: admin
          image: ghcr.io/your-org/admin:dev
          ports: [ { containerPort: 8000 } ]
---
apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: ravkav-geo-dev
spec:
  selector: { app: admin }
  ports:
    - name: http
      port: 80
      targetPort: 8000
