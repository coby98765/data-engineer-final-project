apiVersion: apps/v1
kind: Deployment
metadata:
  name: parser
  namespace: ravkav-geo-dev
spec:
  replicas: 1
  selector: { matchLabels: { app: parser } }
  template:
    metadata: { labels: { app: parser } }
    spec:
      containers:
        - name: parser
          image: ghcr.io/your-org/parser:dev
          ports: [ { containerPort: 8000 } ]
---
apiVersion: v1
kind: Service
metadata:
  name: parser
  namespace: ravkav-geo-dev
spec:
  selector: { app: parser }
  ports:
    - name: http
      port: 80
      targetPort: 8000
