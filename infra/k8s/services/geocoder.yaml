apiVersion: apps/v1
kind: Deployment
metadata:
  name: geocoder
  namespace: ravkav-geo-dev
spec:
  replicas: 1
  selector: { matchLabels: { app: geocoder } }
  template:
    metadata: { labels: { app: geocoder } }
    spec:
      containers:
        - name: geocoder
          image: ghcr.io/your-org/geocoder:dev
          ports: [ { containerPort: 8000 } ]
---
apiVersion: v1
kind: Service
metadata:
  name: geocoder
  namespace: ravkav-geo-dev
spec:
  selector: { app: geocoder }
  ports:
    - name: http
      port: 80
      targetPort: 8000
